---
sidebar_position: 7
sidebar_label: 容器管理工具
---
## Portainer

```shell
docker pull portainer/portainer
```

```shell
docker volume create portainer_db
docker run -d --name docker-web -p 9001:9000 -v /var/run/docker.sock:/var/run/docker.sock -v portainer_db:/data portainer/portainer
```